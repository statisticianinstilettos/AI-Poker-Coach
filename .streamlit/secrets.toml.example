# API Keys
OPENAI_API_KEY = "sk-your-openai-key-here"
OPIK_API_KEY = "your-opik-key-here"

# MongoDB Connection String
MONGODB_URI = "mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority"

# Authentication Configuration
cookie_key = "your-random-cookie-key"  # A random string for cookie encryption

# User Credentials
[credentials.usernames.admin]  # Replace 'admin' with desired username
email = "admin@example.com"
first_name = "Admin"
last_name = "User"
password = "your-hashed-password"  # Use stauth.Hasher.hash('your-password') to generate

[credentials.usernames.test_user]  # You can add multiple users
email = "test@example.com"
first_name = "Test"
last_name = "User"
password = "your-hashed-password"  # Use stauth.Hasher.hash('your-password') to generate 